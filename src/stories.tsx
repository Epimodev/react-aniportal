import { createElement } from 'react';
import { storiesOf } from '@storybook/react';
import AniPortal from './index';

storiesOf('MultiAutoComplete', module).add('default', () => <AniPortal>Hello Portal</AniPortal>);
